User changes settings
↓
Settings saved (DB / config layer)
↓
Analyzer runs normally
↓
Aggregator computes risk score
↓
Decision engine applies SETTINGS thresholds
↓
Final decision (allow / warn / escalate)
↓
Logged with policy version
